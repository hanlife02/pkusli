## 目标

- 本次要生成或修改什么演示稿：

## 背景

- 使用场景：
- 目标受众：
- 希望达到的效果：

## 约束

- 默认只修改 `main.typ`、`task.md`、`config.md`。
- 默认不修改 `slides/`、`style/`、`figures/`。
- 优先最小改动。
- 尽量复用仓库现有页面类型与图片素材。

## 验收标准

- `typst compile main.typ` 能通过。
- 演示稿结构完整，包含封面、目录、正文和结束页。
- 内容覆盖用户给定要点。
- 最终结果由用户人工确认是否正确。
